# Vessel segmentations

This folder holds vessel masks paired with the source MRA volumes. Registration converts every mask to binary and transfers the MRA spatial metadata before applying the transforms.
