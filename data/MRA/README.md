# MRA volumes

This folder separates IXI NIfTI MRA volumes from TubeTK MetaImage (`.mha`) volumes. Keep the dataset-specific filenames described in `data/README.md`; the registration and MIP scripts discover cases from those names.
